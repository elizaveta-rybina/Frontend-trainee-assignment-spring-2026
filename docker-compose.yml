services:
  backend:
    build:
      context: ./backend
    environment:
      - PORT=3001
    ports:
      - '3000:3001'

  frontend:
    build:
      context: ./frontend
    ports:
      - '8080:80'
